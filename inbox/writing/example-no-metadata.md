This post has no front matter, no `#` heading, and no `YYYY-MM-DD_` filename prefix - it
exists to prove thebit still publishes a bare markdown file rather than erroring out.

The title below is the humanized filename (`example-no-metadata` -> "Example No Metadata"),
and there's no date badge or tag, since neither could be resolved from anywhere. Front matter
and a date-prefixed filename just make the result nicer; they were never required.
