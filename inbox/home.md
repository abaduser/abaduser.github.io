---
title: thebit
url: https://example.invalid/
heading: Replace this with your one-line hero headline.
---
Replace this paragraph with a couple of sentences about who you are and what this site is
about - it's the bio blurb that renders under the big headline at the top of the page. This
file is read directly by thebit (not part of the writing/work collections), so it's the one
place all site-level copy and identity lives.

Optional front matter keys, add them only if you want them (an unset/absent key is treated
as "off", not an empty value - leave the line out entirely rather than blank):

- `github` / `mastodon` - social links shown in the nav; omitted nav items just don't render.
- `work_heading` (default "Selected work") / `writing_heading` (default "Writing").
- `writing_intro` - a short italic line under the writing heading.
- `writing_all_href` / `writing_all_label` (default "all →") - an "all" link next to the
  writing heading, e.g. once there's a dedicated writing archive page to point it at.
